'use strict';

var request = require('supertest'),
    app = require('../lib/app'),
		chai = require('chai'),
		expect = chai.expect;

describe('Feedback', function() {
    it('allows me to report a bug');
    it('allows me to suggest a feature');
});
