'use strict';

var request = require('supertest'),
    app = require('../lib/app'),
		chai = require('chai'),
		expect = chai.expect;

describe('Browse', function() {
    it('disable fields during refresh');
});
